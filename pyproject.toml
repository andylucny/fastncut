[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-package"
version = "0.1.0"
description = "Example Python package without src layout"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]